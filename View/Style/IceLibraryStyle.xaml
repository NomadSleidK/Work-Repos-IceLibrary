<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:tools="clr-namespace:Ascon.Pilot.Theme.Tools;assembly=Ascon.Pilot.Theme"
                    xmlns:controls="clr-namespace:Ascon.Pilot.Theme.Controls;assembly=Ascon.Pilot.Theme">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/Ascon.Pilot.Theme;component/Themes/StandardControls.xaml"/>
        <ResourceDictionary Source="/Ascon.Pilot.Theme;component/Themes/sharedresources/SharedResources.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <Style TargetType="Label" x:Key="TitleLabel" >
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="Margin" Value="10 5 10 5"/>
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="VerticalAlignment" Value="Bottom"/>
        <Setter Property="FontFamily" Value="{DynamicResource TitleFontFamily}"/>
        <Setter Property="Background" Value="{DynamicResource NormalBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource NormalForeground}"/>
    </Style>

    <Style TargetType="Button" x:Key="DeleteButton" >
        <Setter Property="FontWeight" Value="DemiBold"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Margin" Value="10 5 10 5"/>
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="Background" Value="#FFE45757"/>
        <Setter Property="FontFamily" Value="Bahnschrift"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Height" Value="35"/>
        <Setter Property="Height" Value="35"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.7" />
                <Setter Property="Background" Value="#FFE0E0E0" />
                <Setter Property="BorderBrush" Value="#FFA0A0A0" />
                <Setter Property="Foreground" Value="#FF808080" />
                <Setter Property="Cursor" Value="No" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="Button" x:Key="DownloadButton" >
        <Setter Property="FontWeight" Value="DemiBold"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Margin" Value="10 5 10 5"/>
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="Background" Value="#FF4E608D"/>
        <Setter Property="FontFamily" Value="Bahnschrift"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Height" Value="35"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.7" />
                <Setter Property="Background" Value="#FFE0E0E0" />
                <Setter Property="BorderBrush" Value="#FFA0A0A0" />
                <Setter Property="Foreground" Value="#FF808080" />
                <Setter Property="Cursor" Value="No" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="Label" x:Key="InfoLabel">
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="Margin" Value="10 0 10 0"/>
        <Setter Property="FontSize" Value="18"/>
        <Setter Property="FontWeight" Value="DemiBold"/>
        <Setter Property="Foreground" Value="{DynamicResource NormalBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource NormalForeground}"/>

    </Style>

    <Style TargetType="CheckBox" x:Key="NormalCheckBox">
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="Background" Value="{DynamicResource NormalBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource NormalForeground}"/>
        <Setter Property="FontWeight" Value="DemiBold"/>
    </Style>

    <Style TargetType="TextBlock" x:Key="LinkTextBlock">
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="Margin" Value="10 5 10 5"/>
        <Setter Property="Foreground" Value="{DynamicResource HyperlinkForeground}"/>
        <Setter Property="FontWeight" Value="DemiBold"/>
        <Setter Property="TextDecorations" Value="Underline"/>

    </Style>

    <Style TargetType="TextBox" x:Key="SearchTextBox" BasedOn="{StaticResource TextBoxStyleBase}">
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="Margin" Value="10 5 10 5"/>
        <Setter Property="Height" Value="20"/>

    </Style>

    <Style TargetType="TextBlock" x:Key="SearchMessage" BasedOn="{StaticResource DefaultTextBlockStyle}">
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="Margin" Value="25 0 0 0"/>
        <Setter Property="IsHitTestVisible" Value="False"/>
        <Setter Property="Width" Value="auto"/>
        <Setter Property="FontWeight" Value="DemiBold"/>

        <Setter Property="Background" Value="{DynamicResource NormalBackground}"/>
        <Setter Property="Foreground" Value="Gray"/>

    </Style>

    <Style TargetType="TextBlock" x:Key="EmptyElementMessage" BasedOn="{StaticResource DefaultTextBlockStyle}">
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="IsHitTestVisible" Value="False"/>
        <Setter Property="Width" Value="auto"/>
        <Setter Property="FontStyle" Value="Italic"/>

        <Setter Property="Background" Value="{DynamicResource NormalBackground}"/>
        <Setter Property="Foreground" Value="Gray"/>

    </Style>

    <Style TargetType="TextBlock" x:Key="TitleInfoTextBlock" BasedOn="{StaticResource DefaultTextBlockStyle}">
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="TextWrapping" Value="WrapWithOverflow"/>
        <Setter Property="Width" Value="auto"/>
        <Setter Property="Height" Value="auto"/>
        <Setter Property="Margin" Value="10 5 10 5"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="Background" Value="{DynamicResource NormalBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource NormalForeground}"/>

    </Style>

    <Style TargetType="TextBlock" x:Key="MediumTitleInfoTextBlock" BasedOn="{StaticResource DefaultTextBlockStyle}">
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="TextWrapping" Value="WrapWithOverflow"/>
        <Setter Property="Width" Value="auto"/>
        <Setter Property="Height" Value="auto"/>
        <Setter Property="Margin" Value="10 5 10 5"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="FontFamily" Value="{DynamicResource MediumFontFamily}"/>
        <Setter Property="Background" Value="{DynamicResource NormalBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource NormalForeground}"/>

    </Style>

    <Style TargetType="TextBlock" x:Key="InfoTextBlock" BasedOn="{StaticResource DefaultTextBlockStyle}">
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="TextWrapping" Value="WrapWithOverflow"/>
        <Setter Property="Width" Value="auto"/>
        <Setter Property="Height" Value="auto"/>
        <Setter Property="Margin" Value="10 5 10 5"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Background" Value="{DynamicResource NormalBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource NormalForeground}"/>

    </Style>

    <Style TargetType="TextBlock" x:Key="DescriptionTextBlock" BasedOn="{StaticResource DefaultTextBlockStyle}">
        <Setter Property="VerticalAlignment" Value="Stretch"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="TextWrapping" Value="WrapWithOverflow"/>
        <Setter Property="Width" Value="auto"/>
        <Setter Property="Height" Value="auto"/>
        <Setter Property="Margin" Value="10 5 10 5"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontFamily" Value="{DynamicResource MediumFontFamily}"/>
        <Setter Property="Background" Value="{DynamicResource NormalBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource NormalForeground}"/>

    </Style>

    <Style TargetType="Rectangle" x:Key="NormalRectangle">
        <Setter Property="Fill" Value="{DynamicResource NormalForeground}"/>
        <Setter Property="StrokeThickness" Value="4"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
    </Style>

    <Style TargetType="TreeView" x:Key="NormalTreeView">
        <Setter Property="Margin" Value="10 5 10 5"/>
        <Setter Property="Background" Value="{DynamicResource NormalBackground}"/>
    </Style>

    <Style TargetType="TabControl" x:Key="NormalTabControl">
        <Setter Property="Margin" Value="10 5 10 5"/>
        <Setter Property="Background" Value="{DynamicResource NormalBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource NormalForeground}"/>
    </Style>

    <Style TargetType="TextBlock" x:Key="NormalTextBlock">
        <Setter Property="Background" Value="{DynamicResource NormalBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource NormalForeground}"/>
    </Style>

    <Style TargetType="DataGrid" x:Key="NormalDataGrid">
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="VerticalAlignment" Value="Stretch"/>
        <Setter Property="AutoGenerateColumns" Value="True"/>
        <Setter Property="IsReadOnly" Value="True"/>
        <Setter Property="Margin" Value="10 5 10 5"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="BorderBrush" Value="{DynamicResource NormalForeground}"/>
        <Setter Property="Background" Value="{DynamicResource NormalBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource NormalForeground}"/>
        <Setter Property="ColumnWidth" Value="*"/>
    </Style>

    <Style TargetType="DataGridColumnHeader" x:Key="NormalDataGridColumnHeader">
        <Setter Property="BorderBrush" Value="{DynamicResource NormalForeground}"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Margin" Value="3 3 3 3"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type DataGridColumnHeader}">
                    <Grid>
                        <Border 
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        Padding="{TemplateBinding Padding}">
                            <ContentPresenter 
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                            RecognizesAccessKey="True"/>
                        </Border>
                        <Path 
                        x:Name="SortArrow"
                        Visibility="Collapsed"
                        Data="M0,0 L1,0 0.5,1 z"
                        Fill="Black"
                        Stretch="Fill"
                        Width="8"
                        Height="6"
                        Margin="5,0,0,0"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        RenderTransformOrigin="0.5,0.4"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="SortDirection" Value="Ascending">
                            <Setter TargetName="SortArrow" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="SortArrow" Property="RenderTransform">
                                <Setter.Value>
                                    <RotateTransform Angle="180"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="SortDirection" Value="Descending">
                            <Setter TargetName="SortArrow" Property="Visibility" Value="Visible"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

    </Style>

</ResourceDictionary>